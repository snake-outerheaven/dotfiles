# ~/.tmux.conf — Jarvis
# Configuração com workflow inspirado no i3 (tiling window manager).
# Copiar para ~/.tmux.conf no Jarvis.
#
# Mapeamento conceitual i3 -> tmux:
#   i3 "workspace"  -> tmux "window"
#   i3 "container"  -> tmux "pane"
#   i3 Mod key      -> tmux prefix (Ctrl+a)

# --- Prefixo (equivalente ao "Mod" do i3) ---
# Ctrl+b é o padrão, mas conflita com outros programas de terminal.
# Trocando para Ctrl+a (mais acessível, mesmo padrão do screen).
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# --- Comportamento geral ---
set -g mouse on              # permite usar o mouse pra selecionar painel/redimensionar/scroll
set -g history-limit 10000   # mais linhas de histórico por painel
set -g base-index 1          # janelas (workspaces) começam em 1, não em 0
setw -g pane-base-index 1
set -g renumber-windows on   # renumera janelas automaticamente ao fechar uma no meio
set -sg escape-time 0        # remove delay ao apertar Esc (útil pra quem usa vim)
set -g focus-events on       # repassa eventos de foco pros programas dentro do painel

# =========================================================
# TILING — divisão de painéis (equivalente ao split do i3)
# =========================================================
unbind '"'
unbind %
bind | split-window -h -c "#{pane_current_path}"   # like i3 "splith" (Mod+h)
bind - split-window -v -c "#{pane_current_path}"   # like i3 "splitv" (Mod+v)

# --- Novo workspace (janela) — equivalente a Mod+Enter (abrir terminal) no i3 ---
bind Enter new-window -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"        # mantido por compatibilidade

# =========================================================
# NAVEGAÇÃO — foco entre painéis, vim/i3-like (h j k l)
# =========================================================
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# --- Mover o painel focado (equivalente a Mod+Shift+h/j/k/l no i3) ---
bind H swap-pane -s '{left-of}'
bind J swap-pane -s '{down-of}'
bind K swap-pane -s '{up-of}'
bind L swap-pane -s '{right-of}'

# --- Navegar entre janelas/workspaces sem prefixo repetido (Mod+scroll no i3) ---
bind -n M-Left  previous-window   # Alt+Seta esquerda: workspace anterior
bind -n M-Right next-window       # Alt+Seta direita: próximo workspace

# =========================================================
# LAYOUTS — equivalente aos layouts do i3 (splith/splitv/tabbed/stacking)
# =========================================================
bind e select-layout even-horizontal   # i3 "splith" layout
bind E select-layout even-vertical     # i3 "splitv" layout
bind t select-layout tiled             # grade — todos os painéis visíveis
bind m select-layout main-vertical     # um painel principal + coluna lateral (i3 "master/stack")
# Espaço (default do tmux) já cicla entre todos os layouts disponíveis

# =========================================================
# FULLSCREEN / ZOOM — equivalente a Mod+f (fullscreen) no i3
# =========================================================
bind f resize-pane -Z

# =========================================================
# RESIZE MODE — equivalente ao "resize mode" do i3 (Mod+r)
# Entra no modo, usa h/j/k/l repetidamente sem precisar do prefixo de novo,
# sai com Escape ou Enter.
# =========================================================
bind R switch-client -T resize
bind -T resize h resize-pane -L 5 \; switch-client -T resize
bind -T resize j resize-pane -D 5 \; switch-client -T resize
bind -T resize k resize-pane -U 5 \; switch-client -T resize
bind -T resize l resize-pane -R 5 \; switch-client -T resize
bind -T resize Escape switch-client -T root
bind -T resize Enter  switch-client -T root

# =========================================================
# FECHAR — equivalente a Mod+Shift+q (kill window) no i3
# =========================================================
bind x kill-pane
bind X kill-window

# --- Recarregar config sem reiniciar sessão (equivalente a Mod+Shift+r no i3) ---
bind r source-file ~/.tmux.conf \; display-message "Config recarregada"

# =========================================================
# BARRA DE STATUS — equivalente à i3bar
# =========================================================
set -g status-style bg=black,fg=white
set -g status-left "#[fg=green,bold]#S #[fg=white]| "        # nome da sessão
set -g status-left-length 20
set -g status-right "#[fg=cyan]#{pane_title} #[fg=yellow]%d/%m %H:%M"
set -g status-interval 5

# Lista de janelas no estilo "workspaces" do i3: número + nome
setw -g window-status-format " #I:#W "
setw -g window-status-current-format " #I:#W "
setw -g window-status-current-style bg=blue,fg=white,bold
